{
		// extends继承的配置文件
    "extends": "../../tsconfig.json",
    "compilerOptions": {
        "outDir": "../../dist/client",
    },
		// references：子项目中依赖的其它子项目；
    "references": [
        { "path": "../common" }
    ]
}
